import { Inter, Lusitana, Manrope } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({ subsets: ['latin'], weight:'700'});
export const manrope = Manrope({ subsets: ['cyrillic'], weight:'500'});